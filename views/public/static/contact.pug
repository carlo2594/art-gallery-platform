extends /layouts/public

block content
  section.contact-section.py-5
    .container
      .row.justify-content-center
        .col-lg-8
          h1.display-4.fw-bold.mb-4 Contáctanos
          if success
            .alert.alert-success(role='alert') Tu mensaje fue enviado. Te responderemos pronto.
          if error
            .alert.alert-danger(role='alert')= error
          p.lead.mb-4 Si tienes preguntas o deseas ponerte en contacto con Galería del Ox, escríbenos a nuestro correo de soporte.

          .contact-info.mb-4
            span.contact-label Correo:
            a.contact-email-link(href="mailto:soporte@galeriadelox.com" aria-label="Enviar correo a soporte@galeriadelox.com") soporte@galeriadelox.com

          h2.h5.mt-5.mb-3 Envíanos un mensaje
          form(action="/api/v1/contact" method="POST")
            //- Honeypot anti-spam
            input(type='text' name='company' tabindex='-1' autocomplete='off' aria-hidden='true' style='position:absolute;left:-9999px;height:1px;width:1px;padding:0;margin:0;border:0;')
            .mb-3
              label.form-label(for="name") Nombre
              input#name.form-control(type="text" name="name" required value=formData && formData.name)
            .mb-3
              label.form-label(for="email") Correo electrónico
              input#email.form-control(type="email" name="email" required value=formData && formData.email)
            .mb-3
              label.form-label(for="message") Mensaje
              textarea#message.form-control(name="message" rows="5" required)= (formData && formData.message) || ''
            button.btn.btn-primary(type="submit") Enviar mensaje
