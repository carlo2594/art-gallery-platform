extends ../layouts/public

block content
  // HERO SECTION
  section.hero 
    .container.hero-container
      .row.p-4.pb-0.pe-lg-0.pt-lg-5.align-items-center.hero-row
        .col-lg-7.p-3.p-lg-5.pt-lg-3.hero-content.hero-text        
          p.lead.hero-description
            | Galería del Ox nace de un proyecto familiar y se convierte en un espacio vivo de inspiración, expresión y conexión. Aquí el arte no solo se observa, se siente, se comparte y se descubre.
            br
            br
            | Desde obras originales hasta experiencias sensoriales, cada rincón de la galería está pensado para celebrar la diversidad creativa y hacer del arte un lenguaje común.
          .d-grid.gap-2.d-md-flex.justify-content-md-start.mb-4.mb-lg-3.hero-cta
            a.btn.btn-primary.btn-lg.px-4.me-md-2.fw-bold.text-uppercase(href="/obras") Ver el arte
        .col-lg-5.d-flex.justify-content-end.align-items-center.hero-image-container
          img.hero-image(
            src="/img/Ox-hero-image-landing.jpg",
            alt="Galería del Ox",
            style="max-width:100%;height:auto;border-radius:1.5rem;"
          )


  // Muro de galería (efecto Pinterest con tamaño relativo a cm)
  section.gallery-wall.my-5
    .container
      h2.h4.mb-4.text-center.gallery-wall-title Muro de Galería
      .gallery-wall-grid
        each a in artworks
          figure.gallery-wall-item(
            style=`--wcm:${a.width_cm}; --hcm:${a.height_cm}; --ratio:${a.width_cm} / ${a.height_cm};`
          )
            img.gallery-wall-img(src=a.imageUrl, alt=a.title, loading="lazy")
            if a.title || (a.artist && a.artist.name)
              figcaption.gallery-wall-caption
                if a.title
                  div.caption-title= a.title
                if a.artist && a.artist.name
                  div.caption-artist por #{a.artist.name}

  // Sección para invitar artistas
  section.join-artists.position-relative
    img.join-artists-bg.img-fluid.rounded-4(src="/img/join-artists-bg.jpg", alt="Join Artists Background")
    .join-artists-card.card.position-absolute.top-50.start-50.translate-middle.shadow-lg
      // Imagen superior opcional (puedes quitar si no quieres)
      // img.card-img-top(src="/img/join-artists-bg.jpg", alt="Card image", class="rounded-top-4")
      .card-body.text-center.p-4
        h5.card-title.fw-bold.mb-3 Discover Art That Speaks to You
        p.card-text.fs-5.mb-4 Explore a curated collection of contemporary artworks from emerging and established artists worldwide.
        a.join-artists-btn.btn.btn-primary.rounded-pill.fw-bold(href="/obras") Explore Gallery
