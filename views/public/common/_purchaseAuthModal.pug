//- Lightweight wrapper that reuses the generic auth modal with a dedicated ID
- var modalId    = 'purchaseAuthModal'
- var title      = 'Crea una cuenta para consultar'
- var message    = 'Para consultar la compra de esta obra necesitas una cuenta. Inicia sesión o regístrate para continuar.'
- var returnTo   = (typeof returnTo !== 'undefined' ? returnTo : (`/artworks/${artwork && (artwork.slug || artwork._id)}`))
include /public/common/_authRequiredModal.pug

