//- views/public/common/_authRequiredModal.pug
//- Modal reutilizable para exigir autenticaciÃ³n en acciones (e.g., favoritos)
//- ParÃ¡metros opcionales: modalId, title, message, returnTo, loginHref, signupHref

- var _id        = typeof modalId !== 'undefined' ? modalId : 'authRequiredModal'
- var _title     = typeof title !== 'undefined' ? title : 'Necesitas iniciar sesión'
- var _message   = typeof message !== 'undefined' ? message : 'Para continuar, inicia sesión o regístrate.'
- var _returnTo  = typeof returnTo !== 'undefined' ? returnTo : '/'
- var _loginHref = typeof loginHref !== 'undefined' ? loginHref : '/login'
- var _signupHref= typeof signupHref !== 'undefined' ? signupHref : '/signup'

.modal.fade.auth-required-modal(id=_id tabindex='-1' aria-labelledby=`${_id}Label` aria-hidden='true' data-bs-backdrop='static' data-bs-keyboard='false')
  .modal-dialog
    .modal-content
      .modal-header
        h1.modal-title.fs-5(id=`${_id}Label`)= _title
        button.btn-close(type='button' data-bs-dismiss='modal' aria-label='Cerrar')
      .modal-body
        p.mb-0= _message
      .modal-footer
        a.btn.btn-outline-secondary(href=`${_signupHref}?returnTo=${encodeURIComponent(_returnTo)}`)
          i.fas.fa-user-plus.me-2
          | Registrarme
        a.btn.btn-primary(href=`${_loginHref}?returnTo=${encodeURIComponent(_returnTo)}`)
          i.fas.fa-right-to-bracket.me-2
          | Iniciar sesión
        button.btn.btn-light(type='button' data-bs-dismiss='modal')
          | Cancelar







